#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <stdio.h>

int _putchar(char c);
int _printf(const char *format, ...);
int print_char(va_arg());
int print_string(va_arg());
int print_int(va_arg(()));

#endif
